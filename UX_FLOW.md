# UX Flow – Kompatibilitätsverweis

**Status:** Superseded
**Aktive Source of Truth:** `docs/flows/UX_FLOW.md`
**Grund für den temporären Root-Pfad:** `tests/test_ux_journey.py::test_documented_decisions_exist` liest diesen Pfad direkt. Tests dürfen im aktuellen Dokumentationsauftrag nicht geändert werden.

Der aktive UX-Flow dokumentiert weiterhin den **Minimal-Change-Plan**, den nicht implementierten `MediaRecorder`-Weg, die PDF-Erstellung über `window.print()` und die Entscheidung **keine Migration** für den bestehenden Flow einzuführen. Inhaltliche Pflege findet ausschließlich unter `docs/flows/UX_FLOW.md` statt.
